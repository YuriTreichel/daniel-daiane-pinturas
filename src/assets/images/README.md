# Diretório de Imagens (src)

Imagens colocadas aqui podem ser importadas diretamente nos seus arquivos `.tsx` para que o Vite as otimize automaticamente.

### Exemplo:
```tsx
import logo from './assets/images/logo.png';

function Header() {
  return <img src={logo} alt="Logo" />;
}
```
